import { Drawer } from "@mui/material";
import styled from "styled-components";

const SearchContainer = styled(Drawer)`
  width: 100% !important;
  display: flex !important;
  align-items:center !important;
`

export { SearchContainer }